{
  "filesystem": {
    "enabled": true,
    "comment": "File read/write sandboxed to MCP_FS_ROOT (defaults to project root via process.cwd()). Write tools policy-gated to pro.",
    "transport": "stdio",
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-filesystem", "${MCP_FS_ROOT:-CWD}"],
    "env": {}
  },
  "fetch": {
    "enabled": true,
    "comment": "Fetch any URL and return its content. No API key needed.",
    "transport": "stdio",
    "command": "npx",
    "args": ["-y", "mcp-server-fetch-typescript"],
    "env": {}
  },
  "memory": {
    "enabled": true,
    "comment": "Persistent knowledge graph for semantic memory. No API key needed. Complements/replaces Pinecone.",
    "transport": "stdio",
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-memory"],
    "env": {}
  },
  "sequential-thinking": {
    "enabled": true,
    "comment": "Structured multi-step reasoning before acting. No API key needed. Improves complex task quality.",
    "transport": "stdio",
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"],
    "env": {}
  },
  "search": {
    "enabled": true,
    "comment": "Tavily AI web search — 1,000 free searches/month. Requires TAVILY_API_KEY from app.tavily.com (free). Then flip enabled:true.",
    "transport": "stdio",
    "command": "npx",
    "args": ["-y", "tavily-mcp"],
    "env": {
      "TAVILY_API_KEY": "${TAVILY_API_KEY}"
    }
  },
  "postgres": {
    "enabled": true,
    "comment": "PostgreSQL direct query. Requires DATABASE_URL — get from Supabase dashboard → Settings → Database → Connection string (Transaction mode).",
    "transport": "stdio",
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-postgres", "${DATABASE_URL}"],
    "env": {}
  },
  "notion": {
    "enabled": true,
    "comment": "Notion read/write. Requires NOTION_API_TOKEN — create an integration at notion.so/my-integrations, share pages with it, then flip enabled:true.",
    "transport": "stdio",
    "command": "npx",
    "args": ["-y", "@notionhq/notion-mcp-server"],
    "env": {
      "OPENAPI_MCP_HEADERS": "{\"Authorization\": \"Bearer ${NOTION_API_TOKEN}\", \"Notion-Version\": \"2022-06-28\"}"
    }
  },
  "gcal": {
    "enabled": false,
    "comment": "Google Calendar read/write. Requires GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GOOGLE_REFRESH_TOKEN. Get OAuth2 credentials from console.cloud.google.com → Calendar API → OAuth2 client. Then flip enabled:true.",
    "transport": "stdio",
    "command": "npx",
    "args": ["-y", "@cocal/google-calendar-mcp"],
    "env": {
      "GOOGLE_CLIENT_ID": "${GOOGLE_CLIENT_ID}",
      "GOOGLE_CLIENT_SECRET": "${GOOGLE_CLIENT_SECRET}",
      "GOOGLE_REFRESH_TOKEN": "${GOOGLE_REFRESH_TOKEN}"
    }
  },
  "github": {
    "enabled": false,
    "comment": "GitHub read/write. Requires GITHUB_TOKEN with repo+issues scope. Write tools policy-gated.",
    "transport": "stdio",
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-github"],
    "env": {
      "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
    }
  },
  "playwright": {
    "enabled": false,
    "comment": "Real browser automation — browse, click, screenshot. Requires browser binaries (large, ~300MB). Enable only on non-free Render plans: set PLAYWRIGHT_BROWSERS_PATH=/opt/render/project/src/.playwright",
    "transport": "stdio",
    "command": "npx",
    "args": ["-y", "@playwright/mcp", "--headless"],
    "env": {
      "PLAYWRIGHT_BROWSERS_PATH": "${PLAYWRIGHT_BROWSERS_PATH}"
    }
  },
  "supabase": {
    "enabled": false,
    "comment": "Supabase DB access (read-only). Requires SUPABASE_ACCESS_TOKEN — project management token from supabase.com/dashboard/account/tokens (different from SUPABASE_KEY).",
    "transport": "stdio",
    "command": "npx",
    "args": ["-y", "@supabase/mcp-server-supabase", "--read-only"],
    "env": {
      "SUPABASE_ACCESS_TOKEN": "${SUPABASE_ACCESS_TOKEN}"
    }
  }
}
